# st-api 汇报规则

> 供 `flow-codex-test-report` 引用。业务 spec 汇报仍用 `flow-codex-report`。

## 更新范围

| 文件 | 是否更新 |
|------|----------|
| `task.md` st-api 条目 | 是 |
| `task.md` glm-system-test 服务头部 | 是 |
| `task.md` 完成检查清单 | 勾选「集成测试代码完成」 |
| `开发文档.md` | **否** |
| `发版记录.md` | **否** |
| Apifox | **否** |

## 完成行格式

- 已提交：`完成：YYYY-MM-DD commit {hash}`
- 本地 only：`完成：YYYY-MM-DD local-only`

## spec 条目

位于 `## glm-system-test` 下，格式见 `task-md-maintenance.md` §2.7。
