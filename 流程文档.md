# Flow Skill — 多 Agent 编排工作流文档

> 本文档描述 Flow Skill 系统的整体架构、角色职责和完整工作流程。

---

## 一、系统概览

Flow Skill 是一套**多 Agent 编排工作流**，管理**根 Agent（编排者）**与**子 Agent（执行者）**的协作。

### 两种角色

| 角色 | 职责 | 不做什么 |
|------|------|----------|
| **根 Agent** | 需求拆分、概要设计、派发任务、查看进度、集成测试、接口验证、归档 | ❌ 不编码、不提交、不更新 task.md 完成状态 |
| **子 Agent** | 接收任务 → spec 设计 → 编码（内联审核+测试+提交）→ 汇报 → 知识库维护 | ❌ 不修改其他服务的 spec、不跳过知识库判断 |

### 通信方式

根 Agent 与子 Agent **通过文件解耦通信**，核心文件：

```
{项目根目录}/
├── .flow/
│   ├── config.yaml              ← 项目配置（服务列表、知识库、约定）
│   ├── onboarding.md            ← 子 agent 开发规范（所有服务共享）
│   ├── services.md              ← 服务地图
│   ├── feedback/                ← 线上反馈（独立于 changes，懒创建）
│   │   ├── _index.md
│   │   └── {YYYY-MM-DD}-{slug}/
│   │       ├── 反馈记录.md
│   │       └── 调查报告.md
│   └── changes/
│       └── {需求名}-{YYYYMMDD}/
│           ├── 概要设计.md       ← 整体方案、接口契约、验收标准
│           └── task.md           ← 任务清单（根写入 spec，子 report 勾选完成）
│
{服务目录}/
└── .flow/
    ├── config.yaml              ← 服务配置（指向根目录）
    └── 工作流程.md               ← 本服务工作循环说明
```

---

## 二、命令清单

| 命令 | 角色 | 说明 |
|------|:----:|------|
| `/flow:init` | 根/子 | 初始化 `.flow/` 必要文件 |
| `/flow:design` | 根/子 | 根：概要设计 + task.md + spec proposal/design；子：spec 设计 + 自检（远期） |
| `/flow:assign` | 根 | 分配任务给子 agent（内联执行 / 独立指令包） |
| `/flow:receive` | 子 | 接收任务，加载工作协议，判断当前阶段 |
| `/flow:apply` | 子 | 阶段二编码循环：编码 → 审核 → 测试 → 提交 |
| `/flow:report` | 子 | 汇报结果，更新 task.md（唯一写入者），触发知识库维护 |
| `/flow:status` | 根 | 查看需求进度（spec 粒度） |
| `/flow:verify` | 根 | 跨服务接口契约一致性验证 |
| `/flow:test` | 根 | 集成测试（HTTP / MQ / DB） |
| `/flow:change` | 根 | 需求变更协议 |
| `/flow:archive` | 根 | 归档已完成需求 |
| `/flow:feedback` | 根/子 | 线上反馈调查（`.flow/feedback/`，独立于 change） |
| `/flow:kb` | 根/子 | 知识库维护（`<change-name>` 或 `feedback/{id}`） |
| `/flow:hotfix` | 根 | 创建 hotfix 条目 + spec 目录，走 assign 派发（非 feedback 默认路径） |

---

## 三、场景一：跨服务大需求开发（根指导模式）

适用场景：Tier 2/3 的跨服务需求，需要根 Agent 统筹。

```mermaid
flowchart TD
    U["👤 用户提出需求"] --> INIT["/flow:init<br/>创建 .flow/ 目录 + config.yaml + onboarding.md"]
    INIT --> DESIGN["/flow:design（根）<br/>① 与用户协力讨论服务边界/依赖<br/>② 生成 概要设计.md + task.md<br/>③ 用 spec skills 为每个 spec 创建 proposal + design"]
    DESIGN --> ASSIGN["/flow:assign service-b<br/>按依赖顺序，无依赖先分配<br/>选择：内联执行 / 独立指令包"]

    ASSIGN -->|内联模式| INLINE["根按依赖顺序逐 spec 启动子 agent"]
    ASSIGN -->|独立模式| PKG["根为每个 spec 输出独立指令包<br/>用户手动在服务目录粘贴执行"]

    INLINE --> RECEIVE
    PKG -->|用户手动| RECEIVE

    RECEIVE["/flow:receive spec1<br/>① 检查 .flow/config.yaml（缺则 init）<br/>② 读工作流程.md + task.md 变更通知<br/>③ design.md 已存在 → 直接进入阶段二"]

    RECEIVE --> APPLY["/flow:apply spec1<br/>编码循环：<br/>① 编码（委托 spec_tool）<br/>② 内联审核 agent 检查一致性<br/>③ 单元测试<br/>④ git commit<br/>失败自动重试 ≤3 次"]

    APPLY -->|通过| REPORT["/flow:report<br/>① 结构化汇报<br/>② 更新根 task.md（spec1 ✅）← 唯一写入点<br/>③ 强制知识库判断（不能跳过）"]

    REPORT -->|spec1 完成| NEXT_SPEC{"该服务还有 spec?"}
    NEXT_SPEC -->|有| RECEIVE
    NEXT_SPEC -->|无| SVC_DONE["service-b ✅"]

    SVC_DONE -->|用户告知根| STATUS["/flow:status<br/>查看全局进度<br/>解除 service-a 阻塞"]

    STATUS --> ASSIGN_A["/flow:assign service-a<br/>...同上循环..."]

    ASSIGN_A --> ALL_DONE["所有服务完成 ✅"]

    ALL_DONE --> TEST["/flow:test<br/>集成测试：读取验收标准<br/>HTTP 调用 / MQ 验证 / DB 直查"]
    TEST -->|全通过| VERIFY["/flow:verify<br/>跨服务接口契约一致性验证"]
    VERIFY --> ARCHIVE["/flow:archive<br/>目录补充结束日期<br/>{需求名}-{开始}-{结束}"]

    style INIT fill:#e1f5fe
    style DESIGN fill:#e1f5fe
    style ASSIGN fill:#e1f5fe
    style STATUS fill:#e1f5fe
    style TEST fill:#e1f5fe
    style VERIFY fill:#e1f5fe
    style ARCHIVE fill:#e1f5fe
    style RECEIVE fill:#fff3e0
    style APPLY fill:#fff3e0
    style REPORT fill:#fff3e0
```

### 流程说明

| 阶段 | 执行者 | 命令 | 关键动作 |
|------|:------:|------|----------|
| 初始化 | 根 | `init` | 创建 `.flow/` 目录结构 |
| 设计 | 根 | `design` | 与用户协作：概要设计 + task.md + 每个 spec 的 proposal/design |
| 派发 | 根 | `assign` | 按依赖顺序为每个 spec 生成指令（内联或独立） |
| 接收 | 子 | `receive` | 加载协议、检查阶段、展示变更通知 |
| 编码 | 子 | `apply` | 编码→内联审核→单元测试→commit，最多重试 3 次 |
| 汇报 | 子 | `report` | 更新 task.md、强制知识库判断 |
| 检查 | 根 | `status` | 查看全局 spec 完成进度 |
| 集成测试 | 根 | `test` | 基于验收标准执行 HTTP/MQ/DB 测试 |
| 契约验证 | 根 | `verify` | 检查提供者/消费者接口一致性 |
| 归档 | 根 | `archive` | 补充结束日期，移入 archive 目录 |

---

## 四、场景二：Hotfix 紧急修复

适用场景：生产 bug，快速修复流程。

```mermaid
flowchart TD
    BUG["👤 用户发现 bug"] --> HOTFIX["/flow:hotfix service-a 'bug描述'<br/>① 在 task.md 追加 ### Hotfix 条目<br/>② 用 spec skills 创建 hotfix spec 目录<br/>（proposal.md + design.md）"]
    HOTFIX --> ASSIGN["/flow:assign service-a<br/>派发 hotfix（type=hotfix，跳过设计评审）"]
    ASSIGN --> RECEIVE["/flow:receive hotfix-xxx<br/>design.md 已有 → 直接阶段二"]
    RECEIVE --> APPLY["/flow:apply hotfix-xxx<br/>编码 → 审核 → 测试 → commit"]
    APPLY --> REPORT["/flow:report<br/>更新 task.md hotfix 条目为 ✅<br/>知识库：只记录坑点/根因"]

    style HOTFIX fill:#e1f5fe
    style ASSIGN fill:#e1f5fe
    style RECEIVE fill:#fff3e0
    style APPLY fill:#fff3e0
    style REPORT fill:#fff3e0
```

### 与正常流程的差异

- **根不执行 design**：直接用 spec skills 创建 proposal + design，跳过用户设计评审
- **子 agent 流程相同**：仍然走 receive → apply → report
- **知识库策略**：只记录坑点/根因，不记录业务规则

---

## 四之二、线上反馈调查

适用场景：生产用户反馈、字段异常、接口行为投诉。**与 change / task.md 无关联**。

```mermaid
flowchart TD
    FB["用户反馈"] --> INV["/flow:feedback<br/>① 懒创建 .flow/feedback/<br/>② 反馈记录.md + 调查报告.md"]
    INV --> DECIDE{判定}
    DECIDE -->|bug 要修| FIX["直接改代码 + commit<br/>回填 fix_commit"]
    DECIDE -->|暂不修/设计如此/坑点| KB["/flow:kb feedback/{id}"]
    DECIDE -->|无需沉淀| CLOSE["status=closed"]
    FIX --> KB2["/flow:kb feedback/{id}"]
    KB --> DONE["closed"]
    KB2 --> DONE
    CLOSE --> DONE
```

### 与 Hotfix 编排的差异

| 项 | feedback 路径 | hotfix 编排 |
|----|---------------|-------------|
| 目录 | `.flow/feedback/{id}/` | task.md + OpenSpec |
| 修复 | 直接改代码 | assign → apply → report |
| KB | `/flow:kb feedback/{id}` | report 内判断 |

格式规范见 `flow/docs/schema.md` §11。

---

## 五、场景三：需求变更

适用场景：进行中的大需求发生业务变更。

```mermaid
flowchart TD
    CHANGE["业务需求变化"] --> FC["/flow:change<br/>① 更新 概要设计.md（追加变更记录）<br/>② 更新 task.md（修改 spec + 追加变更通知）"]
    FC -->|下次 receive 时| RECEIVE["子 agent /flow:receive<br/>读到变更通知，展示给用户"]
    RECEIVE --> CHECK{"当前阶段？"}
    CHECK -->|阶段一未完成| REDESIGN["直接修改 spec<br/>重新 /flow:design"]
    CHECK -->|阶段一完成/阶段二未开始| REDESIGN
    CHECK -->|阶段二已完成| NEW_CHANGE["走 /flow:change 流程<br/>（已归档则新建 change）"]

    style FC fill:#e1f5fe
    style RECEIVE fill:#fff3e0
```

### 变更传播机制

- 根 Agent 不主动推送变更，变更通知写入 task.md
- 子 Agent 下次 `receive` 时被动感知
- 根据当前阶段选择处理策略：重新设计 或 新建 change

---

## 六、场景四：用户直接模式

适用场景：Tier 1 的单服务小需求，用户直接在服务目录工作。

```mermaid
flowchart TD
    U["👤 用户进入服务目录"] --> CHECK{"执行任意 flow 命令"}
    CHECK -->|无 config.yaml| INIT["/flow:init（子模式）<br/>配置服务信息 + 向根注册"]
    CHECK -->|已有 config| WORK["子 agent 自主工作循环"]
    INIT --> WORK
    WORK --> RECEIVE["/flow:receive<br/>读 task.md 获取 spec"]
    RECEIVE --> APPLY["/flow:apply<br/>编码循环"]
    APPLY --> REPORT["/flow:report<br/>更新根 task.md（如有根）"]

    style INIT fill:#fff3e0
    style RECEIVE fill:#fff3e0
    style APPLY fill:#fff3e0
    style REPORT fill:#fff3e0
```

---

## 七、核心架构

```mermaid
flowchart LR
    subgraph ROOT["🏗️ 根 Agent（编排者）"]
        direction TB
        R_INIT["init"]
        R_DESIGN["design"]
        R_ASSIGN["assign"]
        R_STATUS["status"]
        R_TEST["test"]
        R_VERIFY["verify"]
        R_CHANGE["change"]
        R_HOTFIX["hotfix"]
        R_ARCHIVE["archive"]
    end

    subgraph CHILD["🔧 子 Agent（执行者）"]
        direction TB
        C_INIT["init"]
        C_RECEIVE["receive"]
        C_DESIGN["design（远期）"]
        C_APPLY["apply"]
        C_REPORT["report"]
        C_KB["kb"]
    end

    subgraph STORAGE["📁 文件通信层"]
        direction TB
        TASK["task.md"]
        CONFIG[".flow/config.yaml"]
        DESIGN_DOC["概要设计.md"]
        ONBOARD["onboarding.md"]
    end

    ROOT -->|"写入（设计/分配）"| STORAGE
    CHILD -->|"写入（汇报）"| STORAGE
    STORAGE -->|"读取"| ROOT
    STORAGE -->|"读取"| CHILD

    style ROOT fill:#e3f2fd
    style CHILD fill:#fff3e0
    style STORAGE fill:#f3e5f5
```

### 关键约束

| 约束 | 说明 |
|------|------|
| **task.md 单一写入者** | 只有 `/flow:report` 勾选 spec 完成状态 |
| **1 spec = 1 commit** | 每个子 agent 只处理一个 spec |
| **文件通信** | 根不直接调用子，子不直接通知根 |
| **根不编码** | 根只做编排，所有代码由子 agent 产出 |
| **子闭环** | 子 agent 内部自动完成审核→测试→提交，不需要用户中转 |

---

## 八、子 Agent 工作循环

子 Agent 的内部三阶段工作循环：

```mermaid
flowchart LR
    RECEIVE["阶段入口<br/>/flow:receive"] --> CHECK{"design.md<br/>存在?"}
    CHECK -->|否| DESIGN_PHASE["阶段一：设计<br/>（远期：自主 spec 设计 + 自检）<br/>当前：根已创建 design.md"]
    CHECK -->|是| APPLY_PHASE["阶段二：编码<br/>/flow:apply<br/>编码→审核→测试→提交"]
    DESIGN_PHASE --> APPLY_PHASE
    APPLY_PHASE --> REPORT_PHASE["阶段三：汇报<br/>/flow:report<br/>更新 task.md + 知识库判断"]

    style RECEIVE fill:#e8f5e9
    style DESIGN_PHASE fill:#fff9c4
    style APPLY_PHASE fill:#ffccbc
    style REPORT_PHASE fill:#e1bee7
```

### 内联 Agent 协作（apply 内部）

```mermaid
flowchart TD
    CODE["编码<br/>（委托 spec_tool）"] --> REVIEW["内联审核 Agent<br/>检查代码与 design.md 一致性"]
    REVIEW -->|❌ 不通过| FIX["修复代码"]
    FIX --> REVIEW
    REVIEW -->|✅ 通过| TEST["单元测试<br/>执行 test_command"]
    TEST -->|❌ 失败| FIX
    TEST -->|✅ 通过| COMMIT["git commit"]
    FIX -->|"累计 >3 次"| STOP["🛑 终止，报告失败"]
```

---

## 九、两种 Agent 启动模式

```
┌──────────────────────────────────────────────────────────────┐
│  独立会话（Independent Session）                              │
│  用户手动在服务目录打开新的 Claude Code 进程                    │
│  · 持久 memory：~/.claude/projects/{项目-服务名}/memory/      │
│  · 拥有独立的 CLAUDE.md、settings、skills                     │
│  · 根 agent 无法自动启动——必须由用户手动操作                   │
├──────────────────────────────────────────────────────────────┤
│  内联 agent（Inline Agent）                                   │
│  由父 agent 通过 Agent tool 启动，运行在父会话的进程内          │
│  · 无持久 memory，随父会话结束而消失                            │
│  · 继承父 agent 的 skills、settings、MCP 工具                  │
│  · 根 agent 和子 agent 都可以启动内联 agent                    │
└──────────────────────────────────────────────────────────────┘
```

---

## 十、文件模板清单

| 模板 | 生成时机 | 内容 |
|------|---------|------|
| `onboarding.md.tmpl` | `init`（根） | 二级架构说明 + 子 agent 开发规范 |
| `config.yaml.tmpl`（根） | `init`（根） | 项目配置、服务列表、知识库、约定 |
| `config.yaml.tmpl`（子） | `init`（子） | 服务配置、inline_agents 配置 |
| `工作流程.md.tmpl` | `init`（子） | 服务级工作循环说明，含测试命令 |
| `概要设计.md` | `design`（根） | 背景、目标、服务边界、接口契约、验收标准 |
| `task.md` | `design`（根） | 按服务分组的 spec 清单（边界+依赖） |
| `proposal.md` | `design`（根/子） | 单个 spec 的背景、目标、方案 |
| `design.md` | `design`（根/子） | 单个 spec 的详细设计 |
| `api.md` | `apply`（子） | 接口变更文档（新增/修改/废弃） |
| `fix.md` | `hotfix`（根） | bug 描述、复现、根因、修复方案 |
| `feedback-*.md.tmpl` | `feedback`（懒创建） | 反馈记录、调查报告、台账 index |
| `feedback-kb-rules.md` | `kb feedback/{id}` | 调查报告 → KB 映射与段落模板 |

---

> 完整设计文档见 `flow-redesign.md`，数据格式规范见 `flow/docs/schema.md`。
