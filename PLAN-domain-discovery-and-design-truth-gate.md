# Flow 领域发现与设计真实性门禁方案

> **实施入口**：本文是领域设计参考，不是任务清单。执行 agent 必须先读取
> [`PLAN-flow-verifiable-automation-implementation.md`](./PLAN-flow-verifiable-automation-implementation.md)，
> 仅在处理 WP1–WP2 时读取本文全文。

## 1. 背景

现有设计 Flow 已要求概要设计包含领域概念、来源、判定条件、影响 spec 和歧义裁决，并检查根概念是否向子 OpenSpec 传导。这能够发现术语缺失、文档矛盾和语义降级，但不能证明领域概念本身符合真实代码、表模型、历史需求和数据语义。

当一个错误领域概念被写入根设计后，概要设计、子 spec、实现和测试可能保持高度一致，却共同实现错误结果。需要在方案设计之前增加独立的领域发现阶段，并由 verifier 检查事实证据，而不是让方案设计自证。

## 2. 目标

1. 将“领域事实发现”与“技术方案设计”拆成两个可独立阻断的阶段。
2. 只要求覆盖本次 change 改变或依赖的业务决策点，不要求穷举整个系统。
3. 为表字段语义、聚合键、覆盖键、状态和转换规则提供可定位证据。
4. 让概要设计、OpenSpec、实现审核和测试场景能够引用稳定的领域事实 ID。
5. 对证据不足或存在冲突且影响实现的问题，在方案设计前停止。

## 3. 两阶段模型

```text
需求 / 澄清 / KB / 历史 change / 当前代码 / 表结构 / 样例数据
                              |
                              v
                    领域发现 DOMAIN_DRAFT
                              |
                    domain verify（事实真实性）
                              |
                              v
                     DOMAIN_VERIFIED
                              |
                              v
                    方案设计 SOLUTION_DRAFT
                              |
                    design verify（消费与传导）
                              |
                              v
                     SOLUTION_VERIFIED
```

## 4. 领域发现产物

新增根 change 产物：

```text
.flow/changes/<change>/domain-model.md
```

### 4.1 必备章节

1. 变更决策点清单；
2. 领域实体和关系；
3. 表与字段语义；
4. 身份、唯一性、聚合与覆盖规则；
5. 状态和状态转换；
6. 输入、存储、输出转换；
7. 正例、边界和反例；
8. 证据索引；
9. 冲突与未决问题；
10. 允许进入方案设计的结论。

### 4.2 领域事实表

```markdown
| Fact ID | 概念 | 精确定义 | 生效条件 | 不生效条件/反例 | 证据 | 影响决策点 |
|---|---|---|---|---|---|---|
| DF-001 | 示例聚合键 | ... | type=... | type!=... 时不得聚合 | code#symbol; schema#column | DP-01 |
```

要求：

- `Fact ID` 在本 change 内稳定；
- 定义必须包含判定条件，不能只写名词解释；
- 条件字段存在枚举或类型分支时，必须写明何时不生效；
- 证据必须能定位到 KB、历史需求、代码符号、表结构、接口定义或经用户确认的决策；
- 单纯引用当前概要设计不构成领域事实证据。

### 4.3 变更决策点

领域发现不扫描整个系统，只覆盖会影响设计分支的决策点：

```markdown
| Decision ID | 问题 | 所需事实 | 若判断错误的影响 | 状态 |
|---|---|---|---|---|
| DP-01 | 哪些记录属于同一逻辑字段 | DF-001, DF-002 | 列丢失、错误覆盖或错误写入 | resolved |
```

典型决策点包括：

- 选行与过滤条件；
- 主键、业务唯一键和定位键；
- 聚合、去重、最深覆盖或优先级；
- 字段只在特定类型下有效；
- `null`、空串、默认值和删除标记的语义；
- 状态是否允许进入某个接口；
- 导入、存储和导出之间是否可逆；
- 多表、多服务之间的权威来源。

## 5. 证据等级

| 等级 | 证据 | 可作为事实依据 |
|---|---|---|
| E1 | 用户明确裁决、已批准需求、权威 KB | 是 |
| E2 | 当前生产代码、数据库约束、接口定义、稳定历史 change | 是，但冲突时需裁决 |
| E3 | 真实脱敏数据样例、日志和已有测试 | 可辅助，不能单独证明普遍规则 |
| E4 | 命名、注释、agent 推断 | 否，只能产生待确认问题 |

证据冲突时必须进入“冲突与未决问题”，不得由 agent 自行选择更方便实现的一项。

## 6. Domain verify

为 `flow-codex-verify` 增加 `verify_mode=domain`，只读执行。

### 6.1 必查项

| ID | 级别 | 检查项 |
|---|---|---|
| DV.1 | ERROR | 每个改变实现分支的 Decision ID 都有事实支撑 |
| DV.2 | ERROR | 每条关键事实包含生效条件和反例/不生效条件 |
| DV.3 | ERROR | E1/E2 证据可定位，且不是当前方案设计自引用 |
| DV.4 | ERROR | 表字段语义与实际 schema/实体/读写代码不存在直接冲突 |
| DV.5 | ERROR | 聚合键、唯一键、覆盖键和状态判断有明确组成字段 |
| DV.6 | ERROR | 影响实现的证据冲突已由用户或权威来源裁决 |
| DV.7 | WARN | 仅有 E3 数据样例支撑的规则存在外推风险 |
| DV.8 | ERROR | 未决问题会改变接口、数据或写入分支却仍声明可设计 |

### 6.2 抽查策略

Verifier 不必重做完整代码探索，但必须独立抽查高风险事实：

- 每个聚合/去重/覆盖规则至少抽查一处实际读写代码或数据库约束；
- 每个条件字段至少检查一个生效分支和一个不生效分支；
- 每个状态规则至少检查入口过滤和最终写入/拒绝位置；
- 每个跨服务权威字段至少检查 provider 与 consumer 的契约。

抽查发现事实错误时输出 ERROR；不能因为文档彼此一致而 PASS。

## 7. Solution design 消费规则

### 7.1 概要设计

概要设计的业务规则、数据访问契约和验收项必须引用 Fact ID：

```markdown
- 基于 DF-001，仅在指定类型和同一业务范围内聚合候选。
```

不得复制一份不带 ID 的同义规则。

### 7.2 OpenSpec

子 spec 的 design 和 requirements 必须声明：

- 消费的 Fact ID；
- 对应实现分支；
- 正向要求；
- 反例和禁止行为；
- 单元测试责任。

### 7.3 Design verify

原有 `verify_mode=design` 调整为检查：

- 方案引用的 Fact ID 存在且已 domain PASS；
- 每个高风险事实至少映射一个设计决策和验收项；
- 子 spec 没有弱化生效条件或丢失反例；
- 方案没有新增未经过 domain verify 的业务推断。

## 8. 与代码审核和测试的传导

### 8.1 Code review

审核报告增加 Fact ID 映射：

```text
DF-001 → Implementation: Class#method → Unit test: Class#testMethod
```

关键事实没有实现分支或反例单测时，review REJECT。

### 8.2 Test design

集成测试不需要为所有 Fact ID 建场景，但必须覆盖会跨越接口、数据库或服务边界的事实。仅属于单方法内部的事实可以标记 integration-N，并引用业务单测证据。

## 9. 实施改动面

预计涉及：

- `flow/templates/`：新增领域模型模板和 verify checklist；
- `flow/docs/schema.md`：登记 domain artifact、状态和 Fact ID 引用；
- `codex/skills/flow-codex-design/`：领域发现阶段；
- `codex/skills/flow-codex-verify/`：domain 模式；
- `codex/skills/flow-codex-review/`：Fact ID 实现映射；
- Claude 对应 commands/skills：同步协议；
- `codex/validate.ps1`：模板、引用和非法自证静态检查。

## 10. 验证方案

### 10.1 静态校验

- 缺 `domain-model.md` 时不能进入 solution design；
- Fact ID 重复、缺条件、缺反例、证据不可定位时失败；
- 概要设计或 OpenSpec 引用不存在的 Fact ID 时失败；
- `来源=当前概要设计` 等自证引用被拒绝。

### 10.2 正反例夹具

至少准备以下通用案例：

1. 条件字段只在某类型生效，其他类型为 `null`；
2. 同名记录按层级覆盖，但不同名记录保持独立；
3. 数据样例表现为唯一，数据库却没有唯一约束；
4. 导出允许多个状态，导入只允许部分状态；
5. KB 与当前代码冲突；
6. 历史需求与现实现不一致；
7. 文档完全一致但领域事实错误。

每个案例必须验证 domain verify 能在方案设计前发现问题。

### 10.3 事故回放

从既有事故抽象出不含业务专名的领域夹具，验证：

- 空编码不会被推断成共同聚合键；
- 条件字段不会脱离类型判断使用；
- 同一输出模板与输入接口的状态范围不一致会进入未决问题；
- 只有 happy path 数据时不能据此宣称规则完整。

### 10.4 Forward test

使用新上下文 agent 分别处理：

- 表字段条件语义需求；
- 状态机需求；
- 跨服务权威字段需求。

评价 agent 是否先形成领域事实和证据，而不是直接生成概要设计。

## 11. 验收标准

- 所有改变实现分支的领域事实都有外部证据；
- domain verify 能发现“文档一致但事实错误”；
- 方案设计不能绕过未决领域问题；
- Fact ID 能追溯到 OpenSpec、实现审核和测试证据；
- 领域发现的额外成本集中在高风险决策点，不演变成全系统建模；
- 当前语义一致性检查被保留，但不再冒充领域真实性检查。
