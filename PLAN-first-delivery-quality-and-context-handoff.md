# 首轮交付质量：全链路验证与上下文交接

## 目标

在保留 design → 审核 → assign/实现 → 集成测试 → 测试环境验收 → 交付的基础上，提高跨服务需求第一次正式送测的质量，减少基本缺陷、重复修复和人工恢复上下文的成本。测试人员文档提供独立补充，不作为发现基本查询、字段、计算和文件问题的必要前提。

本文是 2026-09-15 调查后形成并按用户要求修订的改进计划，不是已经生效的规则或效果验证报告。本轮只授权修订计划，未授权实施脚本、业务测试、CI、提交、推送或安装。执行机制是主体，指标由原始执行产物派生，不以测试人员发现问题后的统计代替开发前置拦截。预算和规则在实施前确认，不得事后修改以取得通过。

本计划沿用 [Design Verify：领域概念校验](./PLAN-design-verify-domain-concepts.md) 的“目标／验收”结构。具体项目、提交、对话及调查边界仅保留在[非分发案例档案](./docs/retrospectives/20260915-attendance-first-delivery-quality.md)，不得复制到分发的 skill、模板或通用示例中。

### 调查形成的工作假说

1. 局部设计、审核和测试没有稳定汇合为当前服务构件组合上的完整业务验收。
2. 部分测试输入和预期沿用了实现假设，可能让两个实现共同通过错误条件。
3. 既有能力调查需要覆盖实际入口、参数转换、依赖版本和目标环境，不能仅确认类或接口存在。
4. 变更后确实发生过重验，但对所有消费者、持久化校验和最终输出的影响分析不完整。
5. 多份“当前状态”和历史正文并存，增加检索、同步及交接成本。
6. 尚无充分证据证明长对话或 Flow 规则本身是主要缺陷原因；需求扩围、已接受取舍、既有问题和环境失败应分别归因。

### 改造范围：不能只修改 skills

| 层次 | 应交付的改进 | 责任与边界 |
|---|---|---|
| Skills 与模板 | 明确业务证明、审核输入、变更影响、证据适用范围及交接职责 | 流程维护者；只固化可复用行为，不加入项目特例，不默认增加状态机 |
| 业务设计与实现 | 确认实际系统能力；首个可运行实现贯通最小业务链；同步上下游约束 | 需求主线与实施者；规则文字不能代替真实调用和代码正确性 |
| 测试支撑 | 稳定的服务启动、数据准备、完成信号、结果读取与清理能力 | 测试支撑维护者；支撑故障单独处置，不靠更换对话重复排障 |
| 测试资产 | 独立预期、真实格式样本、跨层断言、代表性容量输入 | 验收设计者与测试执行者；可本地验证的部分不强依赖整套中间件 |
| 业务知识库 | 公共组件使用边界、旧链路语义、可定位来源和适用版本 | 领域维护者；不堆积事故流水、账号、原始人员照片或敏感数据 |
| 发布与验收执行 | 固定服务构件组合、环境、输入及最终产物，按同一范围收口 | 需求主线；联调发布与正式送测区分，诚实注明未验证不等于交付合格 |
| 效果评估 | 冻结场景分母、记录首次结果、缺陷归因和流程成本 | 主线收集、审核视角复核；不以提交数、测试数或审核轮数替代质量 |

### 核对机制的实现边界

“已有”仅表示本次检查的源码中存在入口或规则，不表示目标需求已经接线并验证成功。

| 机制 | 已有基础与需要补齐的能力 | 实现位置 / 是否超出skills |
|---|---|---|
| 独立预期与反例审核 | 已有领域、设计和实现审核；需稳定执行独立业务推导，不能只核对作者结论 | skills约定＋审核者实际判断；通用脚本不能证明需求完整 |
| 场景、方法、报告与版本映射 | 已有validate-test-cases、validate-test-artifacts和controller；需核查并补齐本计划的实际报告与跨层证据要求 | skills仓共享脚本；不是仅改提示词，不另建第二套场景源 |
| 断言有效性 | 未确认存在可直接复用的定向变异接线，需要实际运行错误变体 | **业务/测试仓测试代码和执行器，超出skills** |
| 开发期完整小链路 | 已有system-test及runner；实际序列化、持久化、消息、文件观测按项目补齐 | **业务服务、测试仓和获授权环境，超出skills** |
| 影响分析与证据失效 | check已有适用性检查；不能宣称已具备完整自动语义依赖发现 | 实施/审核确定影响范围，脚本检查声明范围是否完成重验 |
| 当前构件送测判定 | 已有runner/result verify；需将必要场景与原始证据汇合并输出失败退出码 | 扩展现有validator/runner，skills调用并解释 |
| 平台强制拦截 | 未核实CI必需检查、触发分支、资源、权限和受信报告来源 | **CI流水线与代码平台配置，超出skills且需单独授权** |
| 对话交接 | 当前Codex App支持创建、读取和续接对话；其他宿主不一定有相同工具 | 宿主能力＋交接约定；无工具时人工打开同一入口，不能宣称对话自动保证质量 |
| 计时和量化汇总 | 已有遥测计划，未确认完整实现；不能把计划当成运行能力 | runner/recorder/报告解析器；平台用量只读取实际提供的字段 |

业务测试、测试环境和CI工作在各自仓库或平台实施，不把特定服务代码、配置、账号放进技能分发。当前只修改本计划，不取得上述外部实施权限。

### 核对流程：谁执行、核对什么、如何进入下一步

M1–M6只是本计划的机制索引，不新增controller状态、skill入口或任务编号。

| 机制 / 阶段 | 执行者与输入 | 核对及原始产物 | 失败出口 / 通过去向 |
|---|---|---|---|
| M1 design→verify | 设计者和独立审核视角；原要求、已接受取舍、现有代码和真实格式样本 | 先独立推导操作、输入、预期和反例，再对照设计；记录有依据的遗漏、误报裁决和场景映射 | 核心预期/能力未明确，回design，不实施依赖部分；明确后交receive/assign |
| M2 apply→review | 实施者写测试、审核者核对预期；M1场景及实际转换/依赖 | 运行真实契约测试，关键风险执行有界故障注入/变异；保存基线、变体、预期失败断言和原始报告 | 基线失败先修基线；关键有效错误未被测试捕获则修夹具/断言/设计；通过后继续实现及M3 |
| M3 首个可运行实现起 | 测试执行者；同一逻辑样本、各服务构件、环境和完成条件 | 请求→转换→计算→保存→读取→最终消费端，保存各必要观测点、响应/文件和版本证据 | 环境故障交测试支撑；业务故障交原spec；缺观测保持未验证，有新证据后恢复受影响检查 |
| M4 开发自验及集成测试 | 测试执行者；正确基线、实际依赖版本、固定负载及预算 | 先小样本内容，再局部及必要端到端规模；同时检查数据、布局、耗时和资源 | 内容错误或超预算回实现/设计；不能用一个维度抵消另一个；通过后M5 |
| M5 正式送测前 | 既有validator/runner和result verifier；唯一场景源、服务构件映射及原始报告 | 检查必要场景真实执行、skip、关键变体、M3/M4证据、版本和未提交差异；机器生成送测判定及退出码 | 必要项失败、未执行、过期或缺证据不得判可送测；修复后回相应M1–M4；全部满足再正式交测试人员 |
| M6 测试环境→交付 | 主线及独立验收视角；合格候选、实际部署、补充测试 | 确认部署构件一致、最终范围完成，保留首次失败并回收根因与成本 | 新问题回原change及受影响验证；前次PASS不豁免后续变化 |

M1/M2尽早发现遗漏，M3是开发自己的验证，不是交给测试人员后才运行。M5只证明已约定范围和已检查风险，不能保证未知缺陷为零。

### M2：验证测试是否真的能发现错误

1. 从M1风险选择少量错误类型，如漏过滤、空值错误、次序改变、保存拒绝、输出缺失或布局越界，不默认全仓变异或引入新中间件。
2. 当前正确候选先运行同一测试集，基线必须PASS；基线失败不能用于评价变体捕获能力。
3. 在获授权的隔离临时源码/构建副本应用变体，或使用明确的测试故障替身；不改用户活动工作树、共享数据或正式环境，不提交变体。隔离方式遵守项目约束，不擅自增加分支/长期工作区。替身只证明对应边界，不冒充真实存储或网络。
4. 每个变体绑定风险、目标位置、输入、预期失败断言、时间/资源预算、构件和报告。初版可用少量JUnit及显式变体脚本，不预先指定新框架；引入第三方工具前核实兼容性与实际缺口。
5. 结果区分caught（预期业务断言失败）、survived（有效错误未发现）、invalid（编译失败或不可执行）、equivalent（有依据证明业务等价）、inconclusive（环境/预算等不可判定）。编译失败不算发现业务错误。
6. 自动计算caught/(caught+survived)，同时列出其他类别与风险覆盖。关键有效错误必须被捕获；等价/无效判定须复核，不得静默排除未知项制造满分。
7. 存活变体先调查测试输入和断言，再修测试或设计；保留修订前后结果。通过仅证明选定错误类型可被发现。

### M3：同一样本经过真实连接处

- 实际请求反序列化、字段复制、跨服务参数和SQL绑定按风险走真实代码，字段存在不等于传递正确。
- 计算输出经过真实结果校验与序列化；保存契约本地验证不冒充数据库事务验证，事务/消息风险在获授权环境执行。
- 检查导出时生成真实文件并断言数据、合并、图片归属及完整边界；任务成功、有图片资源、有锚点都不足以证明最终内容。
- 各服务绑定实际构件，同一逻辑输入与身份贯通；需要ID映射时显式保存。异步使用可观察完成信号和超时预算，不以固定sleep证明完成。
- 首个实现先跑小链路，后续扩展既有runner。依赖未实现时允许局部测试，但不能把链路未验证写成可送测。

### M5：本地约束与CI强制的区别

skill要求调用与读取结果，脚本以非零退出码拒绝不完整证据，仍属于本地执行约束，不能阻止其他工具或人工绕过。若目标包含“平台强制”，还需在skills之外完成：

1. 核实目标CI和代码平台的必需检查、分支、权限及运行资源，不假定现有配置满足。
2. 受控执行器检出精确候选、运行适用测试及同一validator，保存原始报告；跨服务绑定候选清单，不只检验单仓HEAD。
3. 接受本次受控执行的报告，不仅信任作者可手写的PASS JSON；缺报告、过期、未运行或流水线故障不能返回成功。
4. 将检查接入相应合入/正式送测流程的必需状态，实际验证失败时平台阻断。允许联调不等于送测通过；管理员覆盖属于显式例外，不承诺绝对不可绕过。
5. CI/权限/测试仓改动需另行授权。本轮不配置。未接入时只报告“本地核验有效，平台强制未实现”；若用户目标要求平台强制，则此项必须保持未完成。

### 证据如何自动成为量化结果

复用现有test-cases.yaml、manifest、证据目录及controller。design先在既有验收章节形成业务场景，正式测试设计承接稳定ID及来源；必要schema扩展修改原协议与validator，不维护两套可执行场景清单。

| 产物 | 采集方式 | 自动派生 | 仍需语义判断 |
|---|---|---|---|
| M1发现 | 审核记录来源、场景和处置，脚本核对引用 | 编码前有效遗漏、误报、发现阶段 | 是遗漏还是已接受取舍 |
| M2运行 | 执行器读取测试报告、变体清单、退出码和失败断言 | 捕获/存活/无效/未知及风险覆盖 | 预期正确性、变体等价性 |
| M3/M4运行 | runner采集输入指纹、构件、阶段结果、数据/文件及计时 | 链路失败、内容差异、预算结果 | 有争议的环境和业务归责 |
| M5核验 | validator连接唯一场景源、报告和版本 | 缺场景、skip、旧证据、候选判定 | 影响范围是否完整 |
| 修复与交接 | 关联原finding、候选、重验及交接回读 | 二次遗漏修复、发现前移、重复补述 | 真扩围与原要求缺陷 |
| 成本 | 现有工具日志；若接入遥测由runner/recorder写事件 | 墙钟、测试、返工、等待时间 | 证据缺失保持未知 |

模型解释结果，不手填通过数、退出码或耗时。Codex对话工具用于组织与读取，不能代替业务执行器或CI门禁。累计输入token不等于唯一上下文长度；未提供的用量保持未知。

### 各阶段的行为改造

| 阶段 | 必须产生的业务证据 | 候选落点 |
|---|---|---|
| design | 当前目标、已接受取舍、真实输入到最终结果的路径；手算或独立来源预期；高风险前提的小范围验证；规模与外部依赖盘点 | flow-codex-design、领域/概要设计模板 |
| 设计审核 | 先从原要求与既有系统推导反例，再核对设计；识别作者假设、能力不兼容和常用路径遗漏 | flow-codex-verify、审核清单 |
| receive / assign | 每个 spec 的提供方、消费者、公共能力及完整链路收口归属；先贯通最小业务链，再扩展矩阵 | receive、assign 的交接输入；保留既有单仓 spec |
| apply / review | 同一业务样本穿过转换、计算、校验、保存和消费端；检查设计正确性而非只确认照设计实现 | apply、review、check |
| change | 列出变化的业务承诺、全部受影响消费者、仍有效证据及必须重跑范围；接受风险保持显式，不反复当成未批准缺陷 | change、check；复用现有产物 |
| 集成测试 | 设计阶段先有验收场景；首个实现即检查最小链路可运行；最终按服务构件组合执行必要场景 | test-design、test、test-verify；对现有前置规则进行明确兼容调整后才实施 |
| 测试环境验收 | 按真实请求和配置核对持久结果、页面与文件；正确性小样本和容量样本分开；测试人员 Excel 做差异补充 | system-test、已有环境操作手册 |
| 交付 | 一个当前状态入口汇总范围、版本、首次和最终结果、问题处理及边界；操作后回读实际状态 | report、check、交接模板 |

“先贯通最小业务链”是既有 specs 的实施顺序，不另建永久任务、后台机制或替代中间件。公开能力开关、锁和版本隔离是否保留，仍依据真实失败场景判断，不能为了精简机械删除。

### 对话组织试点

优先试验“稳定主线＋独立业务审核／测试视角”，不将所有阶段强制拆成固定数量对话。小改动可合并职责，跨服务计算、兼容和高成本输出采用独立视角。新对话或子 agent 仅在用户明确授权且项目允许时使用；本文不构成授权。

- 主线维护当前范围、决定和交付状态，负责发出交接和回收结论；用户不需要重复口述整个需求。
- 独立审核从原始业务依据和样本形成判断，再看作者方案与历史 PASS；不得隐去已经确认的范围和取舍。
- 测试预期先于阅读实现结果确定；核实范围后再看实现细节，允许发现设计错误。
- 实施者负责实现与连接验证；独立测试不得静默改业务代码使测试通过。
- 每次交接先验证版本和未提交差异，不凭“同一工程”认定源码一致。
- 同模型、同材料的多个对话仍可能共享盲点；对话数量不作为质量保证。

最小交接内容放到现有入口或既有报告中，不默认新增平台：

| 内容 | 必须保留 |
|---|---|
| 目标与权限 | 本次要回答的问题、允许操作、禁止操作 |
| 当前范围 | 业务目标、非目标、已接受取舍及原始裁决位置 |
| 版本与环境 | 各服务 commit、工作树、未提交差异、构建/镜像、实际环境 |
| 业务路径 | 关键输入、提供方、转换、存储、消费端和负责收口者 |
| 预期与证据 | 样本、独立预期来源、适用版本、已验证及已知失败 |
| 剩余事项 | 未验证项、阻塞、下一步及停止条件 |

当前业务设计只描述有效行为；当前交付状态只维护一个权威入口；历史证据只追加追溯。其他文件引用，不复制同一份“当前状态”。这不要求删除必要的接口、设计和面向接手者的文档。

### 分步实施计划

1. **盘点与规则兼容。** 核实实际skills/脚本、schema、runner、环境和宿主能力，把M1–M6接到已有流程。明确处理test-design当前要求业务提交后进入、verify禁止启动服务等边界：业务场景前置留在design，执行仍交获授权runner，不能暗中绕过现行规则。
2. **Skills仓实施最小增量。** 固化独立预期、跨层验证责任、变更重验和证据判定；扩展原validator而非新建场景源。用缺报告、skip、旧版本和缺消费者证据验证脚本会拒绝。静态校验不能替代模型执行行为验证。
3. **业务/测试仓补齐资产。** 单独获授权后接入M2关键错误变体、M3完整样本及必要观测点，按现有环境约束运行。测试必须证明旧错误触发正确断言，修复后基线通过；不因维护skills取得业务写权限。
4. **实现当前构件汇合。** 原runner与result verify承接M1–M4和必要场景，生成可重建送测判定；独立核验原始报告及适用版本，修复后检查受影响范围。
5. **按最终目标接CI。** 需要平台强制时单独授权流水线、必需检查与资源；用失败、未运行、旧构件和有效候选演示实际阻断/通过。未接入必须明确外部缺口，不能称全流程强制完成。
6. **运行真实试点。** 固定范围与实际版本，按M1/M2→M3/M4→M5执行，M5通过再正式送测；M6回收后续验收。由原始产物生成指标，不手写成功统计。
7. **评估再推广。** 判断问题是否前移、二次修复和交接成本是否减少；静态分发、模型行为、真实业务、平台强制分别给结论，不因一项通过直接推广。

已有相关计划：[领域概念](./PLAN-design-verify-domain-concepts.md)、[测试可执行性](./PLAN-test-executable-readiness-gate.md)、[测试生命周期](./PLAN-test-verify-lifecycle-gate.md)、[执行遥测](./PLAN-flow-execution-telemetry-and-timing.md)。具体改动路径在实施前对当前源文件进行差异确认；本计划不直接变更它们。

## 验收

### 本计划的落地边界

- 具体案例与证据仅进入非分发案例档案；通用计划和后续分发文本使用占位服务与 change 标识。
- 本轮仅修订此计划，不修改案例事实、现有skills、全局安装、业务代码、CI、Git分支或其他既有未提交改动。
- 后续规则实施必须分别验证：静态分发正确性、模型执行行为、真实业务效果；三者不可互相替代。
- 明确哪些问题需要代码、测试支撑或知识库解决，不能把所有责任转化成 skill 提示词。

### 机制验收：先证明会拦截，再讨论统计

| 被验收对象 | 后续授权试验中的失败输入 | 必须实际观察到的结果 |
|---|---|---|
| M1 | 设计省略原要求的关键条件，或假设目标环境不存在的能力 | 有依据的发现阻止依赖部分实施；静态映射齐全不能替代语义判断 |
| M2 | 基线通过后注入有效错误；另设编译失败、等价/环境故障变体 | 有效错误触发预期业务断言，其他类别不冒充捕获；存活关键变体不能报已验证 |
| M3 | 一端合法输出，另一端拒绝、丢字段或错误消费 | 同一完整样本失败，两个孤立单测PASS不能覆盖 |
| M4 | 性能达标但内容错误，或内容正确但超预算 | 相应维度失败，一个维度不能抵消另一个 |
| M5 | 缺场景、skip、过期报告、手写PASS、未执行或构件不匹配 | 机器失败退出码与定位原因，不产生可送测结论 |
| 变更重验 | 改变关键契约但只提供旧结果 | 证据不适用；完成对应重验后才恢复该范围通过 |
| CI（若纳入） | 必需检查失败、缺失、未运行或构件错误 | 平台实际阻断目标合入/送测；未演示则平台强制为UNVERIFIED |
| 交接与汇总 | 新对话仅从交接入口恢复；报告含阻塞及重验 | 正确恢复约束，首次失败保留，计数可从原始产物重建 |

上述是未来机制验收，不是本轮已执行结果。故意错误仅在授权隔离试验中使用，不向测试人员或生产交付错误构件以证明门禁。

### 下个需求的测量契约

以“已约定范围的首轮交付质量”为主目标，以返工和上下文成本为次目标。观察单位是需求及其业务场景，不是 commit、JUnit 方法或对话条数。

**固定时点：**

- T0：设计及验收场景基线确认、业务实现开始前。记录原范围 S0、关键场景、预期来源、目标值和版本。
- Td：开发首次声明所选完整范围实现就绪并执行完整自验的候选。首次失败必须保留，不能只统计修复到全绿后的T1来掩盖开发返工。
- T1：开发声明可以正式送测的第一个候选构件组合。提前联调允许，但不得先向测试人员交付、发现问题后再把它改称联调；若先正式交付，以该事实确定 T1。
- T2：最终候选验收完成。跨服务“同一版本”是固定的服务构件映射，不要求多个仓库具有同一个 SHA。
- T3：质量观察结束：建议至少经过 5 个工作日，且约定验收范围已由测试或独立验收视角执行完毕，两者均满足。若提前停止，逃逸缺陷结论为观察不足。

**范围与分母：**

- S0 的必要场景在看到实现结果前固定，每个有稳定 ID、业务断言、预期来源、关键性和验证层次。
- 用户在 T0 后新增或明确改变业务范围形成增量集合 SΔ，记录时间、原因和影响。真实新增需求不算原范围缺陷；原已约定要求被遗漏，不能改标为新增需求。
- T1 分母包括当时仍有效的 S0 和已接收的 SΔ。后续新增场景单列其首次结果，不回写 T1；修复过程中删除或弱化场景需要有真实范围变更依据，并保留原记录。
- 环境阻塞、未执行和证据缺失不得静默移出分母。分别记录 PASS、BUSINESS_FAIL、ENV_BLOCKED、TEST_INVALID、NOT_RUN，不能强迫不确定项归为业务错误。
- 仅比较经过范围核实的业务断言，不用测试方法数量扩大通过率；同一根因多个表象只计一个根因，受影响场景数另报。

### 从执行机制派生指标

这些结果从M1–M6原始产物派生；开发期拦截是主验证，逃逸缺陷是补充观察。**95%不作为送测放行阈值：M5须满足全部必要条件。** 预算及未来规则在实施前确认，本文不直接改变现有门禁，也不保证未知缺陷为零。

| 指标 | 计算与证据 | 首个试点建议目标 |
|---|---|---|
| 开发期有效拦截 | M1发现、M2存活与M3/M4首次失败按风险/根因关联，误报另列 | 正式送测前闭合；发现数不是越高越好，不奖励故意差实现 |
| 断言有效性 | M2报告caught/(caught+survived)，同时列其余类别与风险覆盖 | 选定关键有效错误全部捕获，仅证明选定风险 |
| Td首轮完整自验通过率 | Td构件上PASS必要场景数/全部必要场景数，阻塞和未跑不剔除 | 观察开发返工，修复前后分开；不作为送测放行阈值 |
| T1送测就绪 | M5核验当前构件组合、全部必要场景和证据 | 全部必要条件通过才可送测，缺失/失败/过期/未执行不能放行 |
| T1 可判定业务通过率 | PASS / (PASS + BUSINESS_FAIL)，同时报告可判定覆盖率 (PASS + BUSINESS_FAIL) / 必要场景数 | 诊断用途，不单独作为通过依据；防止环境失败与业务错误混算 |
| 基本缺陷逃逸 | T1—T3 首次由测试人员/独立验收发现、属于 T1 已约定基本能力的不同根因数 | 0；基本能力范围必须在 T0 具体列出，如条件筛选、核心字段、关键计算、文件内容 |
| 严重缺陷逃逸 | T1—T3 已约定范围内 P0/P1 根因数；按项目现有严重度定义，不临时降级 | 0 |
| 修复一次闭环 | 同一根因修复后因遗漏消费者、持久化、触发或输出再次修复的次数；新增需求和独立新根因不计入 | 关键链路 0 次二次遗漏修复；其他项逐项解释 |
| T2 最终验收完整性 | 固定最终构件组合上必要场景的有效证据；未影响场景可复用，但需版本差异及适用性依据 | 100% 必要场景有有效结果，关键项不得遗留未验证；范围排除单列，不写全量通过 |
| 性能验收 | T0 固定人数/日期/组合/字段/图片数量与字节量/并发、环境、冷暖条件、阶段计时、内存与总时长目标；记录运行次数 | 达到该需求预先确定的业务预算；单次大样本不报告 p95；不预设全业务统一秒数 |
| 内容与性能兼容 | 性能候选与正确基线比对人员/数据/汇总/布局及其他受影响业务；忽略项需明确理由 | 0 个未解释差异；任务成功不能替代最终文件正确 |
| 用户上下文补述 | 已有明确记录仍要求用户重新解释的次数；真正新决定与授权不计入 | 0 次已记录事实的重复口述；每次发生均记录缺失的交接字段 |
| 交接恢复时间 | 收到交接至能准确复述目标、版本、约束并完成首个有效动作的墙钟时间；登录/外部等待单列 | 暂只建基线；不虚构历史值，不先承诺降低百分比 |
| 返工与流程成本 | 设计/编码/测试/环境排障/文档状态维护/等待分开计时；按原要求错误、范围变化、既有问题分类 | 报告绝对时间及占比；有可比基线后再确定改善目标 |
| 模型与上下文成本 | 如可获取，记录实际模型、输入/缓存输入/输出 token、重复检索和执行时间；取不到标 N/A | 同配置、相近规模比较；累计输入不是唯一上下文长度，也不直接等同费用 |

基本缺陷 0 是开发自验目标，不是禁止测试发现问题；发现问题必须记录，不能通过不送测、删除场景、改为“需求变更”或只选简单样本来达标。

若一个场景覆盖多个关键业务断言，全部满足才算 PASS。不同规模的百分比必须同时报告 n/N。性能预算由真实业务可接受等待、资源和并发确定，不由本计划任意给出统一阈值。

### 最小评估记录与责任

在现有需求证据目录和当前状态入口内记录，不新增守护进程或独立数据库。

| 记录 | 最小字段 | 收集 / 复核 |
|---|---|---|
| 场景结果 | 场景ID、范围版本、关键性、输入/预期来源、构件映射、环境、首次结果、最终结果、证据链接 | 测试执行者 / 独立验收视角 |
| 缺陷归因 | 问题ID、根因ID、首次发现阶段、严重度、原要求依据、所属范围、首次修复及二次遗漏 | 主线 / 审核视角；有争议保留，不自行漂白 |
| 活动成本 | 开始结束、活动类别、参与对话、有效动作、外部等待、证据来源 | 主线从已有记录汇总；无法恢复的时间标未知 |
| 交接核验 | 目标/版本/约束是否正确、补问原因、恢复时间、事实遗漏 | 接收者回读 / 主线确认 |
| 结论 | 质量达标/未达标/证据不足；效率改善/未改善/不可判定；后续措施 | 主线汇总，用户确认推广决策 |

效率比较先检查业务规模、变化次数、服务数、模型与测试环境是否可比。单个新需求可以判断绝对质量目标是否达成，通常不足以证明改造造成效率提升。建议连续观察至少 3 个有代表性的需求，再决定扩大对话拆分或继续重构 skills；这仍不是统计上的因果证明。

### 试验通过与推广条件

1. 历史盲测只使用当时生效的要求及修复前构件，隐藏答案但保留用户约束；记录有效发现、误报、用户补述和耗时，不以“猜中已知 bug”代替真实交付验证。
2. 下一需求在 T0 确认目标，先贯通最小链路；独立审核与测试职责经过用户授权，主线自动提供交接。
3. Td及T1首次结果不可覆盖；T2对最终服务构件组合收口；T3结束前不宣称逃逸缺陷为零。不得仅用T1全绿证明首次实现无返工。
4. 质量目标未达成，定位具体阶段并调整，不通过增加文档或审核轮数自动补偿。
5. 质量达成但无可比效率基线，只报告“质量达标，效率改善尚不可判定”。
6. 若拆分只增加重复调查、误报和维护成本，没有更早发现有效遗漏，缩小或调整拆分；不把多对话变成新的强制仪式。
