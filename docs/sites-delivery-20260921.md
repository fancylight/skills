# 工作站点技能交付记录

2026-09-21，单对话实施与自查，无独立审核 Agent。新增 flow-codex-sites，统一 Edge 优先及平台／操作知识复用。core 平台入口覆盖各 Flow 阶段，feedback 保留调查落盘流程，普通站点操作不调用该流程。没有新增 hook、后台服务、worktree 或远程调试端口。

## 部署与资料

公共技能与模板进入原 Codex 安装流程，validate 必需技能清单增加 sites。个人站点目录位于用户主目录 `.flow/worksites/`，不进入 skills 安装覆盖范围。已登记13个平台，包括复杂 GBP 的模块目录与劳务动态上下文。

实际全局安装后共25个 Flow 技能；连续两次安装保留全部19份个人文件，安装后的 sites 文件与源码逐项哈希一致。

旧项目 CDP 索引保留兼容指引，历史内容不批量删除。MyDB 已有 Edge 主操作手册；GBP 私有云查库仍保留旧知识引用，查询未验证不冒充迁移完成。公司地址、账号及项目上下文只保存在个人目录，不打包进公共模板。

## 实际 Edge 验证

| 平台／操作 | 实际结果 | 限制 |
|---|---|---|
| MyDB 测试 MySQL 只读查询 | SELECT 1 返回一行；修正编辑器操作后重复查询通过 | 不代表其他实例与数据库类型已验证 |
| 劳务两组上下文 | 测试项目规则配置与正式项目陌生人页面可定位，观察到不同菜单及角色 | 未主动切租户项目、未验证抓请求或业务修改 |
| GBP | 工作台多模块目录与数据库运维入口实际可见 | 私有云查询缺代表环境；未触发构建发布 |
| ELK | 在近15分钟范围执行唯一标记 KQL，返回无匹配结果 | 不代表真实业务日志关联或时区校准已验证 |
| 堡垒机 | CAS 自动返回已登录资产页 | 未连接资产，未执行终端／Arthas |
| Rancher、ES | 实际显示重新登录／登录页 | Pod日志与ES查询未验收，等待登录 |
| 其他已登记平台 | 来自实际 Edge 标签页盘点 | 常用操作随实际任务补齐 |

MyDB 首次手册复测发现 Ace fill 追加旧语句及自动补全导致多个 textbox。修复为限定 SQL 编辑器、键盘替换、检查完整可见输入，并等待本次列名。包含排障的复测52.489秒；修正后从输入到确认约11.87秒，不含登录和选库，不宣称整体提速百分比。首次探索没有准确起点的时间标未知。

## 框架验证

`codex/validate.ps1` 通过25技能校验。`codex/tests/test-sites-install.ps1` 在 PowerShell 7 和 Windows PowerShell 5.1 通过：临时安装、重复安装资源一致、个人文件保留、新操作不需重装，以及卸载已安装技能后个人资料仍保留。

本次是指令与手册能力，不宣称能强制拦截所有直接浏览器操作；未通过独立新任务评测所有自然语言触发。缺失操作按当前任务补齐，不以结构检查替代真实站点验证。
