# 集成测试改造交付记录（2026-09-21）

本次为单对话实施与自查，没有独立审核 Agent。框架修改基线为 `ffe59ea649bb8f559bde1b78de6a9be2332bc950`，沿用 codex/adapter，不新增 worktree，不修改业务源码和历史提交。

## 已落实

- 测试技能在设计、审核、实现、准备、运行、修复及交付维护事件；脚本记录真实时间并生成阶段、等待、未分类和介入摘要。重复事件幂等，部分交付继续沿用原周期；损坏记录、锁冲突不阻断测试。结束事件刷新现有执行摘要。
- 用例由同一源生成业务总览和具名明细表，旧字符串格式兼容；不把结构完整当成业务审核通过。
- 静态检查可在设计和审核阶段执行；范围拒绝提供具体纠正动作，不自动停止整个任务。真实越界仍拒绝。
- 运行依赖检查使用 runner.check，在启动资源前执行并纳入原指纹；无关业务仓文件可按精确内容及实际构建影响证据排除，不清理整个仓库。
- 已有资源在运行时核实身份与健康后复用；静态预检不冒称就绪。启动进程退出后立即结束等待，不等满健康检查超时。
- 首次断言时间来自真实测试埋点，缺少埋点显示未知；计时不参与 PASS 或预算授权。

## 实际抓住的问题

试点使用的旧自测假定临时路径必须含空格，导致未含空格时误报 Maven 参数丢失；现改为核对完整参数。自定义 ArtifactRoot 后，报告和认证仍硬编码旧目录；现两端都使用实际证据位置，保留哈希与隔离验证。

试点还发现配置中心 JAR 已改变，与历史构件回执不符。这是真实构件问题，重新构建而非豁免哈希。对应启动脚本退出后 runner 仍等待120秒的问题已修正。试点准备脚本的隔离目录、依赖复制和环境文件参数也曾出错，均计入本次耗时，不隐藏这些成本。

## 验证

通过 Codex 24技能校验，以及用例生成、旧格式兼容、具名表错误行宽、计时可控时钟、重复事件、暂停恢复、强退未分类、最终摘要刷新、范围保护、构建输入审核、环境解析/预检、controller 执行恢复等回归。计时测试覆盖 PowerShell 7 和 Windows PowerShell 5.1。

运行回归保留启动失败、结果归因、资源复用、隔离、部分初始化、超时、中断清理及业务错误与清理错误并存的验证。新增非考勤异步订单投影：重复输入、延迟可见、两阶段更正及真实断言埋点通过；依赖失败不会继续启动服务，启动进程退出不会等满超时。

暂存安装覆盖24技能、145文件；两次安装文件哈希一致，新计时依赖存在。

全局安装至 `C:/Users/chenk-u/.agents/skills` 后重复安装，24技能、145文件哈希无变化；计时脚本与源码一致，Codex 技能校验通过。既有 `config.toml` 和 `hooks.json` 内容哈希未改变。

## 跨夜实际试点

- 使用已有测试 worktree，实际业务版本 `165d172e879bc91497e4612b2957d324f0b11920`；原锁定版本为 `61435c19`，没有回退分支。
- 2026-09-21 10:06:18开始，30分钟截止时间保持10:36:18；所选运行于10:28:26完成，总计22分8秒，未追加预算。之后只做结果自查和交付记录。
- LE-01、LE-07a：2个方法、8组输入，0失败、0跳过。更正场景四组结果均由1800变为3000，与已有独立预期一致；最终复核8组和本次数据、进程清理通过。
- 成功运行约206秒：服务及资源启动约100秒，输入准备约5秒，测试约94秒，清理约5秒。其他时间包含构建、框架修复、认证、失败启动和排障间隔，不能用206秒代替全过程耗时。
- 加上结果自查，试点观察周期约25分31秒。部分重试间隔未细分为修复阶段，不能把记录中的暂停时长解释为用户等待或模型活跃时间；本次没有要求用户介入。全过程阶段分类仍需后续真实需求验证。
- 原测试配置与框架文件已恢复；本次构建输出、失败历史及独立试点证据保留在测试仓 `.local/flow-pilot-20260921T020618`，未写 canonical Flow 完成状态。

原始证据：`C:/Users/chenk-u/code/glm/.flow/worktrees/cross-night-system-test-20260930/changes/attendance-cross-night-segment-settlement-20260930/evidence/runs/20260921T022458-f16714ef/`；结构化 runner 结果与自查见该测试仓 `.local/flow-pilot-20260921T020618/runner-result.json`、`pilot-review.json`。

## 边界

这次真实试点复用正式 runner 的 standalone 模式，只验证选中链路；controller 编排恢复由框架回归验证，没有把 standalone 升级为正式需求 PASS。未重跑全部17项或旧CN回归；未验证新的真实需求从设计到全部交付所需时间；旧Java用例未加首次断言埋点，不能提供该指标。不能据此宣称整个流程已彻底解决或给出相对历史需求的提速百分比。

全局安装更新技能、公共脚本及模板；已有项目定制 runner 不被批量覆盖，后续接入需按实际差异更新、审核和认证。后续阶段以真实全过程计时、有效覆盖和介入原因评价效果，不重复用框架 PASS 代替业务进展。
