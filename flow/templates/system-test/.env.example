# Optional UI toolchain (only validated when set)
# NODE14_EXE=
# NODE14_NPM_CLI=
# NODE24_EXE=
# NODE24_NPM_CLI=

# Middleware (runner doctor checks endpoints declared in manifest.dependencies)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=
MYSQL_USER=root
MYSQL_PASSWORD=
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
KAFKA_BOOTSTRAP_SERVERS=127.0.0.1:9092
ELASTICSEARCH_URL=http://127.0.0.1:9200
WIREMOCK_BASE_URL=http://127.0.0.1:18080

# Optional: override orchestrator root (default = parent of this repo)
# FLOW_ORCH_ROOT=

# Project-specific service base URLs — add as needed for each change
# EXAMPLE_SERVICE_BASE_URL=http://127.0.0.1:8080
