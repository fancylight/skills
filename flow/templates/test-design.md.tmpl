# 集成测试设计：{{change_name}}

> 本文只定义测试设计和配置契约；不得记录实际 PASS/FAIL、执行日期、耗时或 runner evidence。

## 授权与配置契约

- authorization ceiling：{{authorization_ceiling}}（默认 `design`，仅用户可提升）
- configurationSource：{{configuration_source}}
- requiredEndpoints：{{required_endpoints}}
- connectivityProbe：{{connectivity_probe}}
- ownership：{{configuration_owner}}

`connectivityProbe` 只能对用户确认的来源执行一次最小只读连接或 metadata 检查；失败输出
`[TEST_CONFIGURATION] BLOCKED` 并停止等待人工配置，不得修改配置或切换来源。

## 场景来源与派生物

`changes/{{change_name}}/test-cases.yaml` 是场景、稳定 ID、required、测试方法、报告类、runner filter
和证据契约的唯一可执行来源。`test-plan.md` 只保留解释性说明，不得维护第二份可执行场景计数或映射。
manifest 必须以 `testCasesContract.path` 引用 `test-cases.generated.json`；该 sidecar、test-plan 受控 generated 区、
runner filter、报告类、计数、integration Y/N、evidence index 和 failure observability 必须由 canonical source
确定性生成并绑定同一 test revision/source hash。任何重复、缺失或漂移都必须在 design/implementation verify 中拒绝。

## TDD.1 测试目标与风险

- 验收范围：{{acceptance_scope}}
- 风险与边界：{{risks}}
- 不测试项：{{non_goals}}

## TDD.2 SUT 与 revision

| 服务/仓库 | 分支 | commit | 启动模块 | 端口 | 配置 |
|---|---|---|---|---|---|
| {{sut_service}} | {{sut_branch}} | {{sut_commit}} | {{sut_module}} | {{sut_port}} | {{sut_config}} |

## TDD.3–TDD.4 拓扑与真实/替身边界

| 调用方 → 依赖 | 真实/替身 | 地址/启动方式 | 请求匹配与响应契约 | 理由 |
|---|---|---|---|---|
| {{caller}} → {{dependency}} | {{boundary}} | {{runtime}} | {{stub_contract}} | {{reason}} |

## TDD.5–TDD.10

记录鉴权上下文、fixture 的预留 ID/seed/cleanup、观测点、验收覆盖策略、SQL 计划和失败归因。每个 integration-Y 场景必须可映射到测试方法与核心断言；外部证据缺失只能登记 BLOCKED，不得自动修改 owning repo。

## Manifest 约束

manifest 必须声明 `requiredEnvBySuite`、`wireMockContracts`、`fixtureSchema`、`runner.command`（token array）和
`testCasesContract.path`。场景派生字段不得在 manifest 根重复维护；由 sidecar 声明 count、report class、filter、
integration Y/N、evidence index 与 `failureObservability`。每个场景绑定稳定 ID、测试类/方法、关联字段、
原始报告/日志/桩/数据库证据路径；证据不足时必须使用 `UNDETERMINED`。
