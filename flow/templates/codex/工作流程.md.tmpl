# {{service_name}} Codex Flow 工作流程

> 本文件由 `$flow-codex-init` 自动生成，持久化在服务目录 `.flow/`。

## 基本信息

- 服务名：{{service_name}}
- 根目录：{{root_path}}
- spec 工具：{{spec_tool}}
- 测试命令：{{test_command}}
- onboarding：{{root_path}}/.flow/onboarding.md

## 单 Spec 执行流程

1. `$flow-codex-receive`：加载明确的 `change_name` 和 `spec_id`。
2. `$flow-codex-apply`：按照 OpenSpec 编码，返回 `REVIEW_REQUEST`。
3. 根 agent 启动同级只读审核 agent，并将审核结果恢复给当前执行 agent。
4. 审核通过后运行 `{{test_command}}`，然后提交一个 spec 的改动。
5. 返回 `REPORT_REQUEST`，等待根 agent 发放报告租约。
6. `$flow-codex-report`：更新根追踪文件并返回 `[REPORT] complete`。

## 并发边界

- 同一个仓库默认只运行一个写入 agent。
- 不在没有报告租约时修改根 `task.md`。
- 分支不匹配或存在未知本地改动时停止，不自行切换或清理。
