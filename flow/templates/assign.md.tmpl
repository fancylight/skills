你好，我是 {{service_name}} 的子 agent。

## 启动指引
请先阅读以下文件：
1. {{onboarding_path}}
2. {{next_tasks_path}}（找到你的任务）

## 当前任务
根 agent 指派任务：{{task_summary}}
{{#if task_id}}
任务号：{{task_id}}（用于 commit message）
{{/if}}

需求：{{task_description}}

## 跨服务上下文
{{cross_service_context}}

## 工作要求
1. 创建 TodoList（使用 TaskCreate）
2. 设计阶段（使用你的 spec 工具{{#if spec_tool}}，如 {{spec_tool}}{{/if}}）
3. 完成后告知用户："设计已完成，请审阅"
4. 用户审阅通过后开始编码
5. 完成验证清单（编译通过、测试通过）
{{#if task_id}}
6. 提交代码（格式：{{commit_format}}）
{{else}}
6. 提交代码
{{/if}}
7. 执行 /flow:report 提交汇报

注意：现在只做设计，不写代码。等用户审阅通过后再编码。

---

## 红线提醒

执行过程中如发现以下情况，**立即停止编码**，向用户报告：
1. 需要修改其他服务的代码或配置
2. 发现接口契约与 next-tasks.md 中的描述不一致
3. 任务范围明显超出上述任务描述
4. 需要删除或大幅修改数据库表结构（尤其是其他服务依赖的表）
5. 需要变更根 agent 已确认的跨服务接口契约