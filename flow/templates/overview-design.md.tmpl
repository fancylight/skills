# {{requirement_title}}

> 本文件由 `/flow:design`（根模式）自动生成，位于 `.flow/changes/{change-name}/概要设计.md`。

---

## 背景
{待根 agent 与用户协力填写}

## 目标
{要达到什么效果}

## 涉及服务

| 服务 | 职责 | 备注 |
|------|------|------|
{{#each services}}
| {{name}} | {{responsibility}} | {{note}} |
{{/each}}

## 领域概念

> **强制**：合同、审核节点、预警类型、身份/权限等须先查 KB 功能域；有则 `来源=kb`，无则 `来源=change`。
> 不得用实现别名（如 `wc`、「正式合同」）代替未定义词条。
> §C 检索标题：`## 领域概念`（本标题）。

| 词条 | 定义 | 不是 | 判定条件 | 来源 | KB 引用 | kb_action | 影响 spec |
|------|------|------|----------|------|---------|-----------|-----------|
| {例：腾讯电子合同} | {一句话业务语义} | {易混淆边界} | {可验证条件，如 contract_type=3 + tenant/project/company/worker} | kb \| change | {source=kb 时填功能域路径或 @feature-xxx} | {source=change 时：待沉淀 \| 无需} | {c{n} 或 —} |

## 歧义裁决

> 需求输入（纪要、多方文档）存在冲突说法时填写；无冲突时写「无未裁决歧义」。
> §C 检索标题：`## 歧义裁决`（本标题）。

| 议题 | 选项 A | 选项 B | 裁决 | 依据 | 回写 |
|------|--------|--------|------|------|------|
| {例：登记预警写哪张表} | {说法 A} | {说法 B} | A \| B \| 其他 | {产品确认 / 纪要 §x / KB ref} | {更新哪条领域概念或验收标准} |

无未裁决歧义。

## 审核 pass / 写库决策表

> **条件 mandatory**：概要设计描述多步审核、状态流转或「通过/驳回」写库时必须填写。
> §C 检索标题别名：`## 审核 pass / 写库决策表` 或 `## 审核 pass`。

| 审核节点 | 前置条件 | 通过动作 | 目标状态 | MUST NOT | owning spec |
|----------|----------|----------|----------|----------|-------------|
| {例：工区主管审核} | {引用领域概念词条} | {既有写路径，如 passWithProjectAudit} | {status / enter_date 等} | {禁止行为} | {cN} |

## 集成 / 联调范围

> §C 检索标题别名：`## 集成 / 联调范围` 或 `## 集成`。

| 范围 | 说明 | 依赖 spec |
|------|------|-----------|
| 首派 \| Phase-2 \| 不在本次 | {可测边界} | {c{n} 或 st-api} |

## 数据访问契约

> **条件 mandatory**：新增或修改列表、分页、报表 SQL，或改变既有查询的 JOIN / 选行 / 过滤逻辑时必须填写；无此类查询写「无」。
> 每个相关服务 OpenSpec `design.md` 必须带本次适用行及允许/禁止的 SQL 形态，不能只写「正式记录优先」这类结果描述。

| 查询入口（服务/接口或任务） | 主表过滤与 JOIN 等值键 | 期望基数与选行语义 | 唯一性/可用索引依据 | 参考服务/SQL 与偏离理由 | 风险形态与 EXPLAIN 验收 |
|---|---|---|---|---|---|
| {如：花名册分页} | {project_id；a.project_id=b.project_id AND …} | {每主表行至多一条；禁止以 max(id) 推断最新} | {唯一键或索引左前缀} | {服务/Mapper；无偏离或已批准理由} | {PageHelper count；无非豁免 DEPENDENT SUBQUERY、关键大表无 ALL；影响 spec=cN} |

## 服务拆分（Spec 矩阵）

> **铁律**：每行恰好一个 Spec、一个服务（一个 git 仓库）。跨仓须多行、c 递增。禁止 `c4（a + b）`。

| Spec | 服务 | 职责 | 依赖 |
|------|------|------|------|
{{#each specs}}
| {{id}} | {{service}} | {{responsibility}} | {{depends}} |
{{/each}}

## 开发顺序

> 与 Spec 矩阵一一对应；格式见 `task-md-maintenance.md` §2.2。

{{#each specs}}
{{@index_1}}. {{id}}（{{service}}，依赖 {{depends}}）
{{/each}}

## 接口契约草稿

{跨服务 API 定义初稿}

## 分支策略

分支：`{{branch_pattern}}/{{change_name}}`，所有服务使用同一分支名。

## 验收标准

{集成测试的依据，必须章节，描述端到端可验证的行为}

## 非目标

{明确不做的事项}

## 变更记录

{flow:change 时追加}
