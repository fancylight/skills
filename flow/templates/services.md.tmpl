# {{project.name}} 服务地图

> 本文件由 `/flow:init` 自动生成。新增服务时请同步更新。

---

## 已纳入编排的服务

| 服务名 | 目录 | 类型 | 职责简述 |
|--------|------|------|---------|
{{#each services}}
| {{this.name}} | `{{this.path}}` | {{this.type}} | {{this.description}} |
{{/each}}

---

## 其他服务（按需补充）

| 服务名 | 目录 | 职责简述 |
|--------|------|---------|
| | | |

> 当子 agent 遇到未列出的依赖时，在此追加。