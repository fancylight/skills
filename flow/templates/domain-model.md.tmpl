# {{requirement_title}} — 领域模型

> 本文件由 `$flow-codex-design` 的领域发现阶段创建，位于
> `.flow/changes/{{change_name}}/domain-model.md`。
>
> 当前状态：`DOMAIN_DRAFT`。本文件不是概要设计、OpenSpec 或技术方案；未经独立
> domain verify，不得据此创建或修改方案产物。

## 变更决策点

> 只登记会改变本 change 实现分支的业务决策点，不扫描整个系统。

| Decision ID | 问题 | 所需事实 | 判断错误的影响 | 状态 |
|---|---|---|---|---|
| DP-001 | {需要裁决的业务问题} | DF-001 | {错误实现、数据或接口影响} | open \| resolved \| blocked |

## 领域实体与关系

| 实体/概念 | 关系或职责 | 边界 | 关联 Decision ID |
|---|---|---|---|
| {实体或概念} | {关系/职责} | {不包含什么} | DP-001 |

## 领域事实

> 每条事实必须有稳定 Fact ID、精确定义、生效条件、不生效条件或反例、可定位证据和
> 影响的决策点。不得把当前概要设计、实现设想或 agent 推断作为事实证据。

| Fact ID | 概念 | 精确定义 | 生效条件 | 不生效条件/反例 | 证据 | 影响 Decision ID |
|---|---|---|---|---|---|---|
| DF-001 | {概念} | {可判定的业务定义} | {何时成立} | {何时不成立或反例} | EV-001 | DP-001 |

## 表与字段语义

| 表/接口字段 | 业务语义 | 允许值或类型 | 空值/默认值语义 | 证据 Fact ID |
|---|---|---|---|---|
| {table.column 或 contract.field} | {语义} | {范围} | {null/默认值含义} | DF-001 |

## 身份、唯一性、聚合与覆盖规则

| 规则 | 组成字段/前提 | 正例 | 反例或禁止行为 | 证据 Fact ID | 影响 Decision ID |
|---|---|---|---|---|---|
| {唯一/聚合/覆盖规则} | {字段和条件} | {成立样例} | {不应聚合或覆盖的样例} | DF-001 | DP-001 |

## 状态与转换

| 对象 | 当前状态/条件 | 允许转换或行为 | 禁止转换或行为 | 证据 Fact ID | 影响 Decision ID |
|---|---|---|---|---|---|
| {对象} | {前置状态/条件} | {允许结果} | {不允许结果} | DF-001 | DP-001 |

## 输入、存储与输出转换

| 输入 | 存储/处理 | 输出 | 保持或转换的语义 | 反例 | 证据 Fact ID |
|---|---|---|---|---|---|
| {输入} | {处理} | {输出} | {规则} | {不适用情况} | DF-001 |

## 正例、边界与反例

| 场景 | 事实或决策 | 期望 | 不应推断的结论 | 证据 Fact ID |
|---|---|---|---|---|
| {正常或边界场景} | DF-001 / DP-001 | {期望} | {禁止的泛化} | DF-001 |

## 证据索引

| Evidence ID | 等级 | 来源 | 定位 | 支撑 Fact ID | 说明 |
|---|---|---|---|---|---|
| EV-001 | E1 \| E2 \| E3 \| E4 | {用户裁决/代码/schema/接口/样例} | {文件#符号、表列、链接或记录} | DF-001 | {证据作用与局限} |

证据等级：E1（用户明确裁决、已批准需求、权威 KB）、E2（当前代码、数据库约束、接口定义、
稳定历史 change）、E3（脱敏样例、日志、已有测试）、E4（命名、注释、agent 推断）。只有 E1
和 E2 能单独作为领域事实依据；E3 只能辅助，E4 只能形成待确认问题。

## 冲突与未决问题

| 问题 | 冲突证据或缺失证据 | 影响 Decision ID | 所需裁决/补证据 | Evidence ID | 状态 |
|---|---|---|---|---|---|
| {问题；无则写“无”} | {冲突或缺失} | DP-001 | {用户或权威来源的下一步} | EV-002 | open \| resolved \| blocked |

## DOMAIN_DRAFT 检查点

- 仅覆盖本 change 的实现决策点。
- 每个 `resolved` Decision ID 均引用至少一个 Fact ID。
- 每个 Fact ID 均有生效条件、不生效条件或反例，以及可定位的外部证据。
- 影响实现的冲突或未决问题未被自行假设为已解决。
- 下一步：请求 `flow-codex-verify verify_mode=domain`。在 verifier 输出 `DOMAIN_VERIFIED` 前，
  停止；不得生成概要设计、OpenSpec、task 或发版产物。
