# 子 Agent 启动指南（二级 Agent 架构规范）

> 本文件由 `/flow:init` 自动生成，位于根目录 `.flow/`，所有子 agent 共享。

---

## 一、二级 Agent 架构

```
根 Agent（orchestrator）
  职责：需求拆分、概要设计、task.md 维护、进度聚合、集成测试、归档
  位置：项目根目录
  命令：/flow:init /flow:design /flow:assign /flow:status
        /flow:verify /flow:test /flow:change /flow:archive

      ↕ 文件通信（task.md / 概要设计.md）
      ↕ 用户手动传递指令包（平台限制，无法自动启动）

子 Agent（executor）
  职责：接收任务、spec 设计、编码实现、内联自检/审核/测试、汇报
  位置：各服务目录
  命令：/flow:init /flow:receive /flow:design /flow:report /flow:hotfix
```

**平台限制**：根 agent 无法自动启动子 agent 独立会话，根→子的启动必须由用户手动操作。

**内联 Agent**：子 agent 可通过 Agent tool 自动启动内联 agent（审核/测试/知识库维护），无需用户介入。

### 根 Agent 行为约束（重要）

**根 agent 的职责是编排和委托，不是自己执行。**

- **绝对禁止**：根 agent 自己读取 design.md、自己编码、自己跑测试、自己执行服务目录下的任何开发命令
- **正确做法**：通过 `/flow:assign` 启动内联 agent 或生成指令包，让子 agent 去执行
- **例外**（根 agent 可以自己做的）：
  - `/flow:design` 根模式：与用户协力完成概要设计和 task.md
  - `/flow:status`：读取 task.md 查看进度
  - `/flow:change`：更新概要设计和 task.md
  - `/flow:archive`：移动文件到归档目录
- **违反判断**：如果根 agent 正在读取某个服务的代码文件或执行该服务的命令，说明它越界了，应立即停止并委托给子 agent

**原因**：根 agent 的上下文是全局编排信息，不应该被具体服务的代码细节污染。子 agent 的上下文是服务级的，更适合执行开发工作。混用会导致上下文浪费和污染。

---

## 二、子 Agent 工作规范

### 提交格式
`{{conventions.commit_format}}`

### 分支规范
`{{conventions.branch_pattern}}`

### 知识库规则
{{#if knowledge_base.enabled}}
- 路径：`{{knowledge_base.path}}`
- 写入前必须列出变更内容并向用户确认
- 触发条件：新业务规则、坑点发现、接口变更
- 不触发：bug 修复、内部重构、代码格式调整
{{else}}
知识库未启用。
{{/if}}

---

## 三、子 Agent 工作循环（三阶段）

### 阶段一：设计

```
/flow:receive
  → 读取本服务 spec 列表（来自根 task.md）
  → 读取概要设计.md + 知识库
  → 检查变更通知

/flow:design（子模式）
  → 为每个 spec 创建 proposal.md + design.md
  → 内联启动设计评审 agent（自检）
  → 告知用户："设计完成，请评审"
  → [等待评审通过]
```

### 阶段二：编码

```
按 spec 依赖顺序逐个执行：
  apply spec
  → 内联审核 agent（代码 vs spec design，不发散）
      通过 → 继续 | 驳回 → 修改 → 重新审核
  → 单元测试
      通过 → 继续 | 失败 → 修复 → 重新测试
  → 下一个 spec
```

### 阶段三：汇报

```
/flow:report
  → 生成结构化汇报
  → 更新根 task.md（勾选完成 spec）
  → 强制知识库维护判断（不可跳过）
```

---

## 四、服务地图

查看所有服务：`{{root_path}}/.flow/services.md`

如需参考其他服务代码或接口，查阅概要设计.md 中的接口契约章节，或联系根 agent 获取协助。
