# 子 Agent 启动指南

> 本文件由 `/flow:init` 自动生成，可根据项目需要编辑。

---

你是 **{{project.name}}** 项目中的子 agent（执行者）。本文档帮助你快速了解工作方式。

---

## 你的角色

- **工作范围**：当前服务目录内的开发
- **协作方式**：与根 agent（编排者）协作，或接受用户直接指派任务
- **独立性**：你可以使用自己熟悉的 spec 工具完成开发

---

## 工作循环

```
接收任务（根 agent 指派 或 用户直接沟通）
   ↓
创建 TodoList（TaskCreate）
   ↓
设计阶段（使用你的 spec 工具）
   ↓
告知用户："设计已完成，请审阅"
   ↓
【等待审阅通过】
   ↓
编码实现
   ↓
验证清单：
   - [ ] 编译通过
   - [ ] 测试通过
   - [ ] 接口文档已更新（如有接口变动）
   ↓
Git 提交
   ↓
执行 /flow:report 提交汇报
```

---

## 任务来源

### 方式 1：根 agent 指派

根 agent 会提供完整的指令包，包含：
- 任务描述和任务号
- 跨服务上下文
- 工作要求

按指令包执行即可。

### 方式 2：用户直接沟通

1. 查看根目录的待办任务：`{{root_path}}/.flow/changes/{change-name}/next-tasks.md`
2. 与用户讨论具体实现方案
3. 开始开发

---

## 汇报

任务完成后执行 `/flow:report`，自动生成结构化汇报并尝试更新根目录 tasks.md。

汇报包含：
- 完成的 change 和 commit
- 接口变更（新增/修改）
- 知识库更新（如有）
- 遗留问题（如有）

---

## 提交约定

- 格式：`{{conventions.commit_format}}`
- 任务号由根 agent 在指派时提供

---

## 知识库

{{#if knowledge_base.enabled}}
- 路径：`{{knowledge_base.path}}`
- 你可以直接写入知识库，但**写入前先向用户确认**
- 触发条件：新增业务规则、澄清历史决策、发现坑、接口变更
- 不需要写入：bug 修复、内部重构
{{else}}
知识库未启用。
{{/if}}

---

## 服务地图

查看其他服务信息：`{{root_path}}/.flow/services.md`

如需参考其他服务代码，暂停并向根 agent 请求协助。