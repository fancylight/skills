> **受众**：开发、测试、运维、发版。**禁止**写入 spec 名、Flow 术语、Java/.vue 文件清单、完整接口 JSON。规则详见 `dev-doc-maintenance.md`。

## 1 需求文档

> 插入需求文档的 Markdown 链接。如不清楚可询问用户。

## 2 需求分析

| 分析项 | 说明 |
| --------- | ------- |
| 发版范围 | |
| 发版时间 | |
| 发版方式 | |
| 背景意义 | |
| 现状 | |
| 需求定义 | |
| 相关规则 | |
| 影响范围 | |
| 关联影响 | |
| 待确定问题 | |
| 已知问题/注意实现 | |

## 3 开发设计

### 3.1 前端

> 可选。有前端改动时填写：**模块/页面级**影响，禁止文件路径清单。无前端改动可删本节。

| 仓库 | 模块/页面 | 用户可见变化 |
| --- | ---- | ---- |
|     |      |      |

### 3.2 后端开发

#### 3.2.1 业务规则

> 可验证的业务语义：公式、边界、异常含义。design 阶段写摘要；report 阶段按实现收敛。

#### 3.2.2 存储与数据

> 表/字段语义、读写策略、历史兼容。完整 DDL 写在 `发版记录.md`，此处不写 SQL 全文。

#### 3.2.3 数据流转

> 配置 → 持久化 → 运行时消费的链路（服务名、接口路径级）。禁止类名与文件路径。

```text
（示例：配置页 → worker-service POST /xxx → 表 foo → Feign GET /yyy → 业务判断）
```

#### 3.2.4 接口设计

> 请求/响应字段类型见 Apifox。此处仅列路径、变更类型与决策性要点。**禁止 JSON 代码块。**

| 序号 | 接口 | 服务名称 | Apifox 接口地址 | 变更类型 | 变更说明 |
| --- | ---- | ---- | ---- | ---- | ---- |
|     |      |      | 待录入 | 🆕新增 / ✏️修改 |      |

**设计要点**（非 Apifox 细节）：

- 校验规则
- 错误码/业务失败语义
- 与旧接口的差异

## 4 上线 checkList

### 4.1 服务-分支

| 序号 | 服务名称 | git 仓库 | 分支名称 |
| --- | ---- | ----- | ---- |
|     |      |       |      |

### 4.2 SQL、配置

| 序号 | 类型 | 内容 |
| -- | --- | -- |
|    |     |      |

---

维护规则见同目录 `dev-doc-maintenance.md`；report 增量更新见 `dev-doc-update-rules.md`。
