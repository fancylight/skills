> **受众**：开发、测试、运维、发版。**禁止**写入 spec 名、Flow 术语、类名堆砌、本地路径、完整接口 JSON。规则详见 `dev-doc-maintenance.md`。

## 1 需求文档

> 插入需求文档的 Markdown 链接。如不清楚可询问用户。

## 2 需求分析

| 分析项 | 说明 |
| --------- | ------- |
| 发版范围 | |
| 发版时间 | |
| 发版方式 | |
| 背景意义 | |
| 现状 | |
| 需求定义 | |
| 相关规则 | |
| 影响范围 | |
| 关联影响 | |
| 待确定问题 | |
| 已知问题/注意实现 | |

## 3 开发设计

### 3.1 前端

> 可选。有前端改动时填写：**模块/页面级**影响，禁止文件路径清单。无前端改动可删本节。

| 仓库 | 模块/页面 | 用户可见变化 |
| --- | ---- | ---- |
|     |      |      |

### 3.2 后端开发

#### 3.2.1 业务规则

> 可验证的业务语义：公式、边界、异常含义。design 阶段写摘要；report 阶段按实现收敛。禁止类名/spec 名。

#### 3.2.2 存储与数据

> 表/字段语义、读写策略、历史兼容。完整 SQL 写在 **§4.2**，此处不要「见发版记录」或本地路径引用。

#### 3.2.3 数据流转

> 配置 → 持久化 → 运行时消费的链路（**服务名、接口路径级**）。禁止类名与文件路径。

```text
（示例：配置页 → attendance-service POST /rule/xxx → 表 rule_xxx
      → attendance-decide 消费规则 → 日级结果落库）
```

#### 3.2.4 接口设计

> 请求/响应字段类型见 Apifox。此处仅列路径、变更类型与决策性要点。**禁止 JSON 代码块。**
> 「服务名称」列填**可部署服务**（非 git 仓库名）。

| 序号 | 接口 | 服务名称 | Apifox 接口地址 | 变更类型 | 变更说明 |
| --- | ---- | ---- | ---- | ---- | ---- |
|     |      |      | 待录入 | 🆕新增 / ✏️修改 |      |

**设计要点**（非 Apifox 细节）：

- 校验规则
- 错误码/业务失败语义
- 与旧接口的差异

## 4 上线 checkList

### 4.1 服务-分支

> **服务名称 = 可部署/运行单元**，不是 git 仓库名。多模块仓须拆多行，`git 仓库` 列可重复。

| 序号 | 服务名称 | git 仓库 | 分支名称 |
| --- | ---- | ----- | ---- |
| 1 | clock-record-generate | glm-attendance | |
| 2 | attendance-decide | glm-attendance | |
| 3 | labor-time-calculate | glm-attendance | |
| 4 | attendance-service | glm-attendance-service | |

### 4.2 SQL、配置

> DDL/SQL **直接写在本节**（可用代码块）。无配置写「无」。禁止「详见发版记录.md / openspec / 本地路径」。

**DDL**（无变更写「无」）：

```sql
-- 示例：ALTER TABLE ...
```

**配置**：无

### 4.3 测试与验收

> **业务验收语义**（验收点 + 期望行为）。禁止：测试类名、本机地址、启动清单、commit hash、spec id。

- （示例）按天区分进出：日级最多 1 次迟到、1 次早退
- （示例）不区分进出且首末时间相同：只判迟到
- （示例）配置保存后查询回显与保存值一致

---

维护规则见同目录 `dev-doc-maintenance.md`；report 增量更新见 `dev-doc-update-rules.md`。
