# 集成测试计划：{{change_name}}

> 本文只定义场景和断言；实际执行结果写入 evidence 与根 `集成测试.md`。
> system-test path: {{system_test_path}}（必须与根 config 中 `type: system-test` 条目一致）

## 人工场景说明

可执行场景只定义在 `changes/{{change_name}}/test-cases.yaml`。本文件不得复制场景 ID、required 状态、测试方法
或可执行计数；设计 agent 可在此记录背景、边界和人工说明，但任何说明不能替代 canonical source。manifest、
runner filter、报告类、计数和 evidence index 均从该文件确定性生成。

{{scenario_notes}}

## 受控场景镜像

<!-- FLOW_TEST_CASES_GENERATED:START -->
<!-- 由 validate-test-cases.ps1 生成；禁止手工编辑。 -->
<!-- FLOW_TEST_CASES_GENERATED:END -->

外部证据契约、evidence index 骨架和 failure observability 映射位于 manifest 引用的
`test-cases.generated.json`；缺失时输出 `[TEST_EXTERNAL_EVIDENCE] BLOCKED`，不得自动修改 owning repo。

## 配置与运行

- 配置来源：{{configuration_source}}
- 最小只读探针：{{connectivity_probe}}
- 唯一 runner 命令：{{runner_command}}

runner 仅在 implementation PASS、execution 授权和配置探针证据一致后运行一次。

## 失败可观测性

本节只记录人工归因说明。可执行的关联字段、报告类/方法、普通/外部 evidence 路径及默认
`UNDETERMINED` 分类必须读取 `test-cases.generated.json`，不得在 Markdown 再维护一份映射。

{{failure_observability_notes}}
